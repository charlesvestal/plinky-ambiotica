# macOS
.DS_Store

# media / captures (diagnostic artifacts, not source)
*.mov
*.m4a
*.wav

# harness build artifacts
ambiotica_port/harness/obj_*/
ambiotica_port/harness/amb_harness*
ambiotica_port/harness/*.o

# generated single-file panel (regenerate via panel/amalgamate.sh)
ambiotica_port/panel/plinky_ambiotica*.cpp
ambiotica_port/panel/*.o

# compiled unit tests (rebuild via panel/tests.sh)
ambiotica_port/panel/euclid_test
ambiotica_port/panel/stepcond_test

# Plinky IDE API reference — third-party (plinky12.com), NOT redistributable.
# Fetch the latest from the site when needed; never commit it.
llm.txt

# generated community-panels submission bundle (rebuilt by panel/amalgamate.sh)
ambiotica_port/panel/dist/

ambiotica_port/panel/plinky_ambiotica_profile.cpp
